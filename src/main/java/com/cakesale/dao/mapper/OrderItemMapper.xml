<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cakesale.dao.OrderItemDAO">
    <insert id="addOrderItem">
        insert into orderitem (order_id, product_spec_id, product_img, product_num, product_name)
        values (#{orderId}, #{productSpecId}, #{productImg}, #{productNum}, #{productName});
    </insert>

</mapper>